const initialState = {
    mood: {},
    loading: false,
    errors: null,
  };
  
  export const moodReducer = (state = initialState, { type, payload }) => {
    switch (type) {
      default:
        return state;
    }
  };
  